import { MailTemplatePipe } from './mail-template.pipe';

describe('MailTemplatePipe', () => {
  it('create an instance', () => {
    const pipe = new MailTemplatePipe();
    expect(pipe).toBeTruthy();
  });
});
