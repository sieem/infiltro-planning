import { DynamicProjectPipe } from './dynamic-project.pipe';

describe('DynamicProjectPipe', () => {
  it('create an instance', () => {
    const pipe = new DynamicProjectPipe();
    expect(pipe).toBeTruthy();
  });
});
